function comprar() {
   const Widht= 800;
   const height= 600;
   const left= (screen.Widht/ 2)-(Widht / 2);
   const top= (screen.height/ 2)-(height/2);
   window.open('pagamento.html','pagamento','widht=$(widht),height=$(height),top=$(top),left=$(left)');
   


}
function finalizada() {
   const Widht= 700;
   const height= 600;
   const left= (screen.Widht/ 2)-(Widht / 2);
   const top= (screen.height/ 2)-(height/2);
   window.open('finalizada.html','finalizada','widht=$(widht),height=$(height),top=$(top),left=$(left)');

  
   }

function voltar(){

 const Widht= 700;
   const height= 600;
   const left= (screen.Widht/ 2)-(Widht / 2);
   const top= (screen.height/ 2)-(height/2);
   window.open('index.html','index','widht=$(widht),height=$(height),top=$(top),left=$(left)');


}


function perfil(){

   const Widht= 700;
     const height= 600;
     const left= (screen.Widht/ 2)-(Widht / 2);
     const top= (screen.height/ 2)-(height/2);
     window.open('perfil.html','perfil','widht=$(widht),height=$(height),top=$(top),left=$(left)');
   } 